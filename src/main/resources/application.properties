### DB ?? ###
##spring.datasource.url=jdbc:mysql://potatoes-db-instance-1.c80r9gb0whb6.eu-north-1.rds.amazonaws.com:3306/bloodRecovery
spring.datasource.url=jdbc:mysql://3.36.128.191:3306/blood
spring.datasource.username=etna
spring.datasource.password=gobong

### JPA ?? ###
spring.jpa.hibernate.ddl-auto=update
spring.jpa.generate-ddl=false
spring.jpa.show-sql=true
spring.jpa.database=mysql
spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect
spring.jpa.properties.hibernate.format_sql=true

### DB ?? ?? ###
src/main/resources/application.properties

## customer host add?? ##
endpoint:
    customer:
        host: https://

#OpenFeign
feign:
    client:
        config:
            default:
                retry-period: 500
                retry-max-period: 1000
                max-attempts: 3
                connectTimeout: 3000
                readTimeout: 30000